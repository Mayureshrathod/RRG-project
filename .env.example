# Relative Rotation Graph (RRG) Dashboard Configuration
# No API keys or credentials are required for the default Yahoo Finance data provider.

# Server Configuration (Optional)
# DASH_HOST=0.0.0.0
# DASH_PORT=8050
# DASH_DEBUG=False
